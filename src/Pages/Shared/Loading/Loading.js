import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center p-3'>
            <button className="btn btn-primary loading normal-case text-white">Loading...</button>
        </div>
    );
};

export default Loading;